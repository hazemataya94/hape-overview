# Save original prompt so direnv can restore it on unload automatically

if [[ -z "${_ORIG_PROMPT+x}" ]]; then
  _ORIG_PROMPT="$PROMPT"
fi

export REPO_ROOT="$PWD"
export PROMPT="%F{green}%n:%F{blue}hape$ "